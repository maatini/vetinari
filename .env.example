# Vetinari — Minimal .env
# At least ONE of the following API keys is required.
# The server will not start without at least one key.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=

# Optional overrides (defaults are sensible)
# LOG_LEVEL=INFO
# DEFAULT_MODEL=gpt-4o-mini
# DEFAULT_TEMPERATURE=0.7
# MAX_TOKENS=2048
# ENABLE_CACHE=false

# Model fallback chain (comma-separated)
# FALLBACK_MODELS=anthropic/claude-3-5-sonnet-20241022,gpt-4o-mini,deepseek/deepseek-chat

# LLM resilience
# LLM_MAX_RETRIES=2
# LLM_RETRY_BASE_DELAY_SECONDS=0.5
# LLM_TIMEOUT_SECONDS=90
# LLM_MAX_CONCURRENT=4
