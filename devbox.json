{
  "packages": [
    "python312",
    "uv@latest"
  ],
  "env": {
    "PYTHONUNBUFFERED": "1"
  },
  "shell": {
    "init_hook": "[ -d .venv ] || uv venv; uv sync --all-groups",
    "scripts": {
      "test": "uv run pytest -q",
      "test-cov": "uv run pytest --cov=src/vetinari --cov-report=term-missing -q",
      "server": "uv run python -m vetinari.server",
      "lint": "uv run ruff check src/ tests/"
    }
  }
}
